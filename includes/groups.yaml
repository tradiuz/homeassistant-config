cameras:
  name: Cameras
  entities:
    - camera.backporch
    - camera.front_porch
    - camera.front_wide
    - camera.side_back

animal_lights:
  name: Animal Lights 
  entities:
    - switch.snake_tank_light_switch

